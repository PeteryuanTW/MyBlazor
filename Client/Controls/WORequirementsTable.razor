@using MyBlazor.Shared.DataClass
<table class="table table-striped" style="width:auto">
    <thead>
		<tr>
			<td>wo</td>
		    @foreach(string machineType in machineTypes.Keys)
			{
				<td>@machineType</td>
			}
			<td>Due Time</td>
		</tr>
    </thead>
	@foreach(WOJobs woJobs in allWOJobs)
	{
		<tr>
			<td>@woJobs.wo</td>
			@foreach(TimeSpan interval in woJobs.processCost)
			{
				<td>@interval</td>
			}
			<td>@woJobs.dueTime</td>
		</tr>
	}
</table>

@code {
	[Parameter]
	public Dictionary<string, int> machineTypes { get; set; } = new Dictionary<string, int>();
	[Parameter]
	public List<WOJobs> allWOJobs { get; set; } = new List<WOJobs>();
}
